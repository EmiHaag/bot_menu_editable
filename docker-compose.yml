services:
  bot:
    build: .
    container_name: bot-menu-whatsapp
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./auth_sessions:/data/auth_sessions
